@model VendedorModel

<h2 class="mb-3">Cadastro de Vendedores</h2>

<div class="row">
    <div class="col-md-6 mx-auto">
        <form asp-controller="Vendedor" asp-action="Cadastro">
            <input type="hidden" asp-for="Id" value="@try{@ViewBag.Vendedor.Id} catch{}" /> @*Se der erro não faça nada!?*@ 

            <div class="form-group">
                <label>Nome</label>
                <input class="form-control" asp-for="Nome" type="text" value="@try{@ViewBag.Vendedor.Nome} catch{}" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Email</label>
                <input class="form-control" asp-for="Email" type="email" value="@try{@ViewBag.Vendedor.Email} catch{}"/>
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-block btn-success">Cadastrar</button>
        </form>
    </div>
</div>